<?xml version="1.0"?>
<!DOCTYPE flight_plan SYSTEM "../flight_plan.dtd">
<flight_plan alt="155" ground_alt="135" lat0="48.8064855" lon0="7.8525385" max_dist_from_home="1000" name="IMAV2026 Medikit" security_height="10" home_mode_height="30">

  <header>
    #define FP_NONE       0
    #define FP_SEARCH     1
    #define FP_DROP       2

    #ifndef SwitchServoOn
    #define SwitchServoOn(_x) {}
    #endif
    #ifndef SwitchServoOff
    #define SwitchServoOff(_x) {}
    #endif
    #define DropOpen SwitchServoOn
    #define DropClose SwitchServoOff

    #ifdef NAV_C
    #ifndef TAG_TRACKING_COORD_TO_M
    #define TAG_TRACKING_COORD_TO_M (1.f / 1000.f)
    #ifdef SITL
    #define jevois_stream(_x) {}
    #endif
    #endif

    static inline bool delay_test_rc(bool test, int delay) {
      static int nb = 0;
        if (test) {
        nb++;
        if (nb == delay) {
          nb = 0;
          return true;
        }
        return false;
      } else {
        nb = 0;
        return false;
      }
    }

    #endif
  </header>
  <waypoints>
    <waypoint name="HOME" lat="48.8064855" lon="7.8525385"/>
    <waypoint name="STDBY" lat="48.8066600" lon="7.8521841"/>
    <waypoint name="LANDPAD" lat="48.8064" lon="7.85256"/>
    <waypoint name="TAG" lat="48.8064" lon="7.85233"/>
    <waypoint name="_HERE" lat="48.8065608" lon="7.8530971"/>
    <waypoint name="SEARCH" x="-45.57" y="386.571"/>
    <waypoint name="RESCUE" x="-53.678" y="371.374"/>
    <waypoint name="R1" x="-59.538" y="394.244"/>
    <waypoint name="R2" x="-58.843" y="388.047"/>
    <waypoint name="R3" x="-57.558" y="381.841"/>
  </waypoints>
  <variables>
    <variable init="0" type="int" var="mission_nb"/>
    <variable init="20." var="goto_height" min="5." max="80." step="0.1"/>
    <variable init="7." var="goto_speed" min="0.5" max="10." step="0.1"/>
    <variable init="5." var="takeoff_height" min="0.5" max="15." step="0.1"/>
    <variable init="4." var="land_speed" min="0.5" max="10." step="0.1"/>
    <variable init="8." var="search_height" min="5." max="80." step="0.1"/>
    <variable init="3." var="search_speed" min="0.5" max="10." step="0.1"/>
    <variable init="20." var="search_radius" min="-1." max="100." step="1.0"/>
    <variable init="4." var="search_sweep" min="5." max="20." step="0.1"/>
    <variable init="1.5" var="drop_height" min="1." max="3." step="0.1"/>
  </variables>
  <modules>
    <module name="nav" type="takeoff_and_landing"/>
    <module name="tag_tracking">
      <define name="TAG_TRACKING_WP" value="WP_LANDPAD"/>
      <define name="TAG_TRACKING_SIM_WP" value="WP_TAG"/>
    </module>
    <module name="agl_dist"/>
    <module name="imav_rescue_people"/>
  </modules>
  <includes>
    <include name="Data" procedure="IMAV2026_data.xml"/>
  </includes>
  <exceptions>
    <exception cond="(!InsideFlight_Area(GetPosX(), GetPosY()) @OR (GetPosAlt() @GT GetAltRef() + 100)) @AND (nav_block @GT IndexOfBlock('Holding point')) @AND (nav_block @LT IndexOfBlock('Kill landed'))" deroute="Standby"/>
    <exception cond="(!InsideKill(GetPosX(), GetPosY()) @OR (GetPosAlt() @GT GetAltRef() + 120)) @AND (nav_block @GT IndexOfBlock('Holding point')) @AND (nav_block @LT IndexOfBlock('Kill landed'))" deroute="Kill landed"/>
    <exception cond="(nav_block @GT IndexOfBlock('Holding point') @AND nav_block @LT IndexOfBlock('Kill landed')) @AND (GpsIsLost() @AND delay_test_rc(RadioControlIsLost(),20) @AND (datalink_time @GT 5))" deroute="Kill landed"/>
    <exception cond="(nav_block @GT IndexOfBlock('Holding point') @AND nav_block @LT IndexOfBlock('Kill landed')) @AND (delay_test_rc(RadioControlIsLost(),20) @AND (datalink_time @GT 15))" deroute="Kill landed"/>
  </exceptions>
  <blocks>
    <block name="Wait GPS">
      <call_once fun="NavKillThrottle()"/>
      <call_once fun="jevois_stream(false)"/>
      <call_once fun="DropOpen()"/>
      <while cond="!GpsFixValid()"/>
    </block>
    <block name="Geo init">
      <while cond="LessThan(NavBlockTime(), 4)"/>
    </block>
    <block name="Holding point" strip_button="H. Point" group="home">
      <call_once fun="NavKillThrottle()"/>
      <attitude pitch="0" roll="0" throttle="0" until="FALSE" vmode="throttle"/>
    </block>
    <block name="Start Engine">
      <call_once fun="NavResurrect()"/>
      <attitude pitch="0" roll="0" throttle="0" until="FALSE" vmode="throttle"/>
    </block>
    <block name="Takeoff" strip_button="Takeoff" strip_icon="takeoff.png" group="home">
      <exception cond="GetPosHeight() @GT takeoff_height @AND mission_nb == FP_NONE" deroute="Standby"/>
      <exception cond="GetPosHeight() @GT takeoff_height @AND mission_nb == FP_SEARCH" deroute="Goto Search"/>
      <exception cond="GetPosHeight() @GT takeoff_height @AND mission_nb == FP_DROP" deroute="Goto Drop"/>
      <!--call_once fun="NavSetAltitudeReferenceHere()"/-->
      <call_once fun="DropClose()"/>
      <call_once fun="NavResurrect()"/>
      <attitude pitch="0" roll="0" throttle="0.1" until="stage_time @GT 2" vmode="throttle"/>
      <call_once fun="NavSetWaypointHere(WP__HERE)"/>
      <stay climb="nav.climb_vspeed" vmode="climb" wp="_HERE"/>
    </block>
    <block name="Standby" strip_button="Standby" strip_icon="home.png" group="home">
      <set var="mission_nb" value="FP_NONE"/>
      <call_once fun="guidance_h_SetMaxSpeed(land_speed)"/>
      <stay wp="STDBY"/>
    </block>

    <block group="mission" name="Start Search" strip_button="Search">
      <set var="mission_nb" value="FP_SEARCH"/>
      <deroute block="Takeoff"/>
    </block>
    <block name="Goto Search">
      <set var="mission_nb" value="FP_NONE"/>
      <call_once fun="NavSetWaypointHere(WP__HERE)"/>
      <call_once fun="guidance_h_SetMaxSpeed(goto_speed)"/>
      <stay wp="_HERE" until="stage_time @GT 3" height="goto_height"/>
      <go wp="SEARCH" from="_HERE" hmode="route" height="goto_height"/>
      <deroute block="Run Search"/>
    </block>
    <block name="Run Search" on_enter="jevois_stream(true)" on_exit="jevois_stream(false)">
      <!-- exception to deroute to drop if validated target -->
      <call_once fun="guidance_h_SetMaxSpeed(search_speed)"/>
      <stay wp="SEARCH" until="GetPosHeight() @LT search_height" vmode="climb" climb="-1.5"/>
      <for var="i" from="1" to="search_radius/search_sweep">
        <circle wp="SEARCH" radius="search_sweep*$i" height="search_height" until="NavCircleCount() @GT 1."/>
      </for>
      <call_once fun="imav_rescue_set_best_wp(WP_RESCUE)"/>
      <deroute block="Drop Medikit"/>
      <!--deroute block="Landing"/-->
    </block>

    <block group="mission" name="Start Drop" strip_button="Drop">
      <set var="mission_nb" value="FP_DROP"/>
      <deroute block="Takeoff"/>
    </block>
    <block name="Goto Drop">
      <set var="mission_nb" value="FP_NONE"/>
      <call_once fun="NavSetWaypointHere(WP__HERE)"/>
      <call_once fun="guidance_h_SetMaxSpeed(goto_speed)"/>
      <stay wp="_HERE" until="stage_time @GT 3" height="goto_height"/>
      <go wp="RESCUE" from="_HERE" hmode="route" height="goto_height"/>
      <deroute block="Drop Medikit"/>
    </block>
    <block name="Drop Medikit" on_enter="jevois_stream(true)" on_exit="jevois_stream(false)">
      <stay wp="RESCUE" until="stage_time @GT 3" height="search_height"/>
      <stay wp="RESCUE" vmode="climb" climb="-0.5" until="(agl_dist_valid @AND (agl_dist_value_filtered @LT drop_height)) @OR (GetPosHeight() @LT 0.5) @OR (!nav_is_in_flight())"/>
      <call_once fun="DropOpen()"/>
      <stay wp="RESCUE" until="stage_time @GT 3" height="goto_height"/>
      <deroute block="Landing"/>
    </block>

    <block name="Land here" strip_button="Land here" strip_icon="land-right.png" group="land">
      <call_once fun="NavSetWaypointHere(WP_LANDPAD)"/>
      <deroute block="Flare"/>
    </block>
    <block name="Landing" strip_button="Land on pad" group="land">
      <call_once fun="NavSetWaypointHere(WP__HERE)"/>
      <call_once fun="guidance_h_SetMaxSpeed(goto_speed)"/>
      <go wp="STDBY" from="_HERE" hmode="route" height="goto_height"/>
      <call_once fun="guidance_h_SetMaxSpeed(land_speed)"/>
      <go wp="LANDPAD" from="STDBY" hmode="route" height="goto_height"/>
    </block>
    <block name="Flare" on_enter="jevois_stream(true)" on_exit="jevois_stream(false)">
      <exception cond="!nav_is_in_flight()" deroute="Kill landed"/>
      <call_once fun="guidance_h_SetMaxSpeed(land_speed)"/>
      <stay climb="2*nav.descend_vspeed" vmode="climb" wp="LANDPAD" until="GetPosHeight() @LT takeoff_height"/>
      <stay wp="LANDPAD" height="takeoff_height" until="stage_time @GT 3"/>
      <stay climb="nav.descend_vspeed" vmode="climb" wp="LANDPAD"/>
    </block>
    <block name="Kill landed">
      <call_once fun="NavKillThrottle()"/>
      <attitude pitch="0" roll="0" throttle="0" until="FALSE" vmode="throttle"/>
    </block>

  </blocks>

</flight_plan>
